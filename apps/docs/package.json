{
  "name": "docs",
  "scripts": {
    "build.server": "npx vite build -c adapters/cloudflare-pages/vite.config.ts",
    "deploy": "npx wrangler pages publish ./dist",
    "serve": "npx wrangler pages dev ./dist"
  },
  "devDependencies": {
    "wrangler": "^2.20.0"
  }
}
